

// .........................add
.addLast(val);
.addFirst(val);
.addAt(idx, val);

// ................................get
.getLast();
.getFirst();
.getAt(idx);

// ..............................remove
.removeLast();
.removeFirst();
.removeAt(idx);
